<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.ai.mapper.AiToolLogMapper">

    <insert id="insert" parameterType="com.ruoyi.ai.entity.AiToolLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_tool_log (user_id, session_id, tool_name, input_args, output, status, duration, create_time)
        VALUES (#{userId}, #{sessionId}, #{toolName}, #{inputArgs}, #{output}, #{status}, #{duration}, sysdate())
    </insert>

</mapper>
