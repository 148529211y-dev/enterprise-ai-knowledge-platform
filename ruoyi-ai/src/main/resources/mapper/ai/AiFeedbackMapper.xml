<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.ai.mapper.AiFeedbackMapper">

    <insert id="insert" parameterType="com.ruoyi.ai.entity.AiFeedback" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_feedback (conversation_id, session_id, question, answer, score, comment, user_id, create_time)
        VALUES (#{conversationId}, #{sessionId}, #{question}, #{answer}, #{score}, #{comment}, #{userId}, sysdate())
    </insert>

    <select id="selectAll" resultType="com.ruoyi.ai.entity.AiFeedback">
        SELECT * FROM ai_feedback ORDER BY create_time DESC
    </select>

</mapper>
