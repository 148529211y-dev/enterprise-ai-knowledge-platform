<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.ai.mapper.AiConversationMapper">

    <insert id="insert" parameterType="com.ruoyi.ai.entity.AiConversation" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_conversation (user_id, session_id, role, content, msg_type, tool_name, tool_args, create_time)
        VALUES (#{userId}, #{sessionId}, #{role}, #{content}, #{msgType}, #{toolName}, #{toolArgs}, sysdate())
    </insert>

    <select id="selectBySession" resultType="com.ruoyi.ai.entity.AiConversation">
        SELECT * FROM ai_conversation
        WHERE user_id = #{userId} AND session_id = #{sessionId}
        ORDER BY create_time ASC
    </select>

    <select id="selectRecent" resultType="com.ruoyi.ai.entity.AiConversation">
        SELECT * FROM (
            SELECT * FROM ai_conversation
            WHERE user_id = #{userId} AND session_id = #{sessionId}
            ORDER BY create_time DESC
            LIMIT #{limit}
        ) t ORDER BY t.create_time ASC
    </select>
</mapper>
